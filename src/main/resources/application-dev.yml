spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/avance_produccion}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
  jpa:
    show-sql: true

logging:
  level:
    "[pe.betaagroindustrial.avance]": DEBUG
    "[org.hibernate.SQL]": DEBUG
